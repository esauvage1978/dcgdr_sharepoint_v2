{% macro view(item) %}
    <a href="{{ path('corbeille_show', {'id': item.id}) }}" class="card m-0">
        <div class="card-body text-center align-middle p-1 bg-primary">
            <span class="{% if item.isEnable is same as(false) %}text-warning text-bold{% endif %}">
                {{ item.name }}
            </span>
        </div>
    </a>
{% endmacro %}

{% macro views(items) %}
    {% import _self as org %}
    <div class="{{ row_lst }} ">
        {% for item in items %}
            {{ org.view(item) }}
        {% endfor %}
    </div>
{% endmacro %}